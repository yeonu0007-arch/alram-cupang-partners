// [검열/수정] 자바스크립트에는 interface가 없으므로 모두 삭제합니다.
// 브라우저 에러 방지를 위해 주석으로만 남기거나 아예 비워둡니다.

/**
 * @typedef {Object} PartnerStats
 * @property {number} clicks
 * @property {number} orders
 * @property {number} revenue
 * @property {number} commission
 * @property {number} conversionRate
 */

// 실제 실행 시에는 아무것도 수출(export)할 필요가 없습니다. 
// App.js에서 'import { PartnerStats }' 부분을 지웠다면 이 파일은 빈 파일이어도 무방합니다.
export const TYPES_PLACEHOLDER = "TYPES_LOADED";
